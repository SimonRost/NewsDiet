name: Scheduled Run (disabled)

on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

jobs:
  run:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Workflow disabled for M0"
